
export function setRaspiPin (raspiDevice, pin, status) {
    console.log(`Set pin ${pin} with status ${status}`);
};

// export function setRaspiPin (raspiDevice, pin, status) {
//     console.log(`Set pin ${pin} with status ${status}`);
// };