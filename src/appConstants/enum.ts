export enum EntityStatus {
    ACTIVE = 'active',
    DELETE = 'delete'
}

export enum DataTypeEnum {
    NUMBER = 'Number',
    STRING = 'String',
    BOOLEAN = 'Boolean',
    DATE = 'Date',
}